diff --git a/Gemfile b/Gemfile
index b43fd2f..6903fef 100644
--- a/Gemfile
+++ b/Gemfile
@@ -43,3 +43,42 @@ group :development, :test do
   gem 'spring'
 end
 
+group :development do
+  gem 'newrelic_rpm'
+  gem 'ruby-prof'
+  gem 'haml-rails'
+  gem 'pry'
+end
+
+group :development, :test do
+  gem 'rspec-rails'
+end
+
+group :test do
+  gem 'rspec-activemodel-mocks'
+  gem 'factory_girl_rails'
+  gem 'cucumber-rails', require: false
+  gem 'capybara'
+  gem 'database_cleaner'
+  gem 'launchy'
+  gem 'simplecov', require: false
+end
+
+group :production do
+  gem 'pg'
+  # Heroku
+  # gem 'rails_12factor'
+end
+
+gem 'bootstrap-sass'
+gem 'autoprefixer-rails'
+gem 'devise'
+gem 'pundit'
+gem 'haml'
+gem 'will_paginate'
+gem 'meta-tags', require: 'meta_tags'
+gem 'rack-timeout'
+gem 'puma'
+
+# Heroku
+# ruby '2.2.2'
