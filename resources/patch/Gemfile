diff --git a/Gemfile b/Gemfile
index 21fc07e..b4e63eb 100644
--- a/Gemfile
+++ b/Gemfile
@@ -38,3 +38,43 @@ gem 'spring',        group: :development
 # Use debugger
 # gem 'debugger', group: [:development, :test]
 
+group :development do
+  gem 'newrelic_rpm'
+  gem 'ruby-prof'
+  gem 'haml-rails'
+  gem 'thin'
+end
+
+group :development, :test do
+  gem 'rspec-rails'
+  gem 'rspec-autotest'
+  gem 'autotest-rails'
+end
+
+group :test do
+  gem 'rspec-activemodel-mocks'
+  gem 'factory_girl_rails'
+  gem 'cucumber-rails', require: false
+  gem 'capybara'
+  gem 'database_cleaner'
+  gem 'launchy'
+  gem 'simplecov', require: false
+end
+
+group :production do
+  gem 'pg'
+  gem 'unicorn'
+  # Heroku
+  # gem 'rails_12factor'
+end
+
+gem 'bootstrap-sass'
+gem 'devise'
+gem 'cancan'
+gem 'haml'
+gem 'will_paginate'
+gem 'meta-tags', require: 'meta_tags'
+
+# Heroku
+# ruby '2.1.2'
+
