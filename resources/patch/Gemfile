diff --git a/Gemfile b/Gemfile
index fc6d3aa..17d8143 100644
--- a/Gemfile
+++ b/Gemfile
@@ -6,5 +6,6 @@ gem 'rails', '4.1.0.rc2'
 # Use sqlite3 as the database for Active Record
-gem 'sqlite3'
+gem 'sqlite3', group: [:development, :test]
 # Use SCSS for stylesheets
 gem 'sass-rails', '~> 4.0.2'
+gem 'bootstrap-sass'
 # Use Uglifier as compressor for JavaScript assets
@@ -32,3 +33,3 @@ gem 'spring',        group: :development
 # Use unicorn as the app server
-# gem 'unicorn'
+gem 'unicorn'
 
@@ -38,3 +39,38 @@ gem 'spring',        group: :development
 # Use debugger
-# gem 'debugger', group: [:development, :test]
+gem 'debugger', group: [:development, :test]
 
+group :development do
+  gem 'newrelic_rpm'
+  gem 'ruby-prof'
+  gem 'haml-rails'
+end
+
+group :development, :test do
+  gem 'rspec-rails'
+  gem 'cucumber'
+end
+
+group :test do
+  gem 'factory_girl'
+  gem 'cucumber-rails', require: false
+  gem 'capybara'
+  gem 'database_cleaner'
+  gem 'launchy'
+  gem 'autotest-rails'
+  gem 'simplecov', require: false
+end
+
+group :production do
+  gem 'pg'
+  # Heroku
+  # gem 'rails_12factor'
+end
+
+gem 'devise'
+gem 'cancan'
+gem 'haml'
+gem 'will_paginate'
+gem 'meta-tags', require: 'meta_tags'
+
+# Heroku
+# ruby '2.1.0'
